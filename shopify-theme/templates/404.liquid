<section class="section" data-reveal>
  <div class="wrap" style="max-width:660px;text-align:center;padding-block:clamp(48px,9vw,120px)">
    <span class="kicker">Erro 404</span>
    <h1 class="h-sec">Página não <span class="it gold">encontrada.</span></h1>
    <p class="lead" style="margin:18px auto 30px;max-width:46ch">A página que procura não existe, mudou de sítio ou saiu de catálogo.</p>
    <p>
      <a class="btn btn--ink" href="{{ routes.root_url }}">Voltar ao início</a>
      <a class="btn btn--ghost" href="{{ routes.all_products_collection_url }}">Ver a coleção</a>
    </p>
  </div>
</section>
